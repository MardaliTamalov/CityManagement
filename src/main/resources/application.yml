server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/city
    username: Mardali
    password: Mardali
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
