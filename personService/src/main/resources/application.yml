server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/person_service
    username: Mardali
    password: Mardali
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    enabled: true
